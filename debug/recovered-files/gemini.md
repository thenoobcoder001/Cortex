# gpt-tui Repository Context

This repository (`gpt-tui`) appears to be a project related to a Text User Interface (TUI) for interacting with GPT or similar AI models. It includes components for:

- **TUI Application:** (`gpt_tui/ui/app.py`) for the main user interface.
- **Providers:** (`gpt_tui/providers/`) likely for different AI model integrations (e.g., Gemini, Groq).
- **Services:** (`gpt_tui/services/`) such as file and shell services.
- **Remote GPT functionality:** (`gpt_remote/`) possibly for running a remote server.
- **Testing:** (`tests/`, `test_*.py`) for various components.
- **Build/Installer scripts:** (`build.ps1`, `build-installer.ps1`) for packaging the application.